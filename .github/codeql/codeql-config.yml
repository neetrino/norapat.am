# CodeQL. Idram Merchant API-ում EDP_CHECKSUM-ը միայն MD5 է (SHA-2-ով չի փոխարինվում)։
# Ռեպոյում այլ createHash('md5') չկա — այս հարցման բացառումը անվտանգ է։
name: norapat-codeql
query-filters:
  - exclude:
      id: js/weak-cryptographic-algorithm
